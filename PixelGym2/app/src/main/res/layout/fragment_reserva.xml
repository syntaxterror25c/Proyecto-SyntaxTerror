<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
        xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:padding="16dp">

    <ImageView
            android:id="@+id/imgReservaActividad"
            android:layout_width="match_parent"
            android:layout_height="200dp"
            android:scaleType="centerCrop"
            android:src="@drawable/im_rec_yoga"
            app:layout_constraintTop_toTopOf="parent" />

    <TextView
            android:id="@+id/tvReservaTitulo"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Nombre de Actividad"
            android:textSize="24sp"
            android:textStyle="bold"
            android:layout_marginTop="16dp"
            app:layout_constraintTop_toBottomOf="@id/imgReservaActividad"
            app:layout_constraintStart_toStartOf="parent" />

    <Button
            android:id="@+id/btnElegirFecha"
            style="@style/Widget.Material3.Button.OutlinedButton"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="24dp"
            android:text="Seleccionar Fecha"
            app:icon="@drawable/ic_calendar_month"
            app:layout_constraintTop_toBottomOf="@id/tvReservaTitulo" />

    <TextView
            android:id="@+id/tvFechaSeleccionada"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Fecha: No seleccionada"
            android:layout_marginTop="8dp"
            app:layout_constraintTop_toBottomOf="@id/btnElegirFecha"
            app:layout_constraintStart_toStartOf="parent" />

    <TextView
            android:id="@+id/tvLabelHoras"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Horas disponibles:"
            android:layout_marginTop="24dp"
            app:layout_constraintTop_toBottomOf="@id/tvFechaSeleccionada"
            app:layout_constraintStart_toStartOf="parent" />

    <Spinner
            android:id="@+id/spinnerHoras"
            android:layout_width="match_parent"
            android:layout_height="50dp"
            android:layout_marginTop="8dp"
            android:background="@android:drawable/btn_dropdown"
            app:layout_constraintTop_toBottomOf="@id/tvLabelHoras" />

    <Button
            android:id="@+id/btnConfirmarReserva"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="RESERVAR AHORA"
            android:layout_marginBottom="16dp"
            android:enabled="false"
            app:layout_constraintBottom_toBottomOf="parent" />

</androidx.constraintlayout.widget.ConstraintLayout>